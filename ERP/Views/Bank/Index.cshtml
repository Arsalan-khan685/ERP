@model IEnumerable<ERP.Models.Bank>

    @{
        ViewData["Title"] = "Index";
    }


    <div class="container p-3">
        <div class="row pt-4">
            <div class="col-md-6">
                <h3 class="text-primary">Bank List </h3>
            </div>
            <div class="col-md-6 text-end">
                <a asp-controller="Bank" asp-action="Create" class="btn btn-primary">
                    <i class="bi bi-plus-lg"></i>&nbsp; Create New Bank
                </a>
            </div>
        </div>
        <br />

        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.BankName)</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.BankName)</td>
                        <td>
                            <a asp-area="" asp-controller="Bank" asp-action="Edit" asp-route-id="@item.BankId" class="btn btn-info">Edit</a>
                            <a href="#" onclick="ConfirmDelete(@item.BankId)" class="btn btn-danger">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

    </div>

    <script>
        function ConfirmDelete(Bankid) {
            if (confirm('Are you sure to delete this Bank?')) {
                window.location.href = '/Bank/Delete/' + Bankid;
            }
        }
    </script>
