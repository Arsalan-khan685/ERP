@model IEnumerable<ERP.Models.PlotSize>

@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-md-6">
            <h3 class="text-primary">PlotSize List </h3>
        </div>
        <div class="col-md-6 text-end">
            <a asp-controller="PlotSize" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-lg"></i>&nbsp; Create New PlotSize
            </a>
        </div>
    </div>
    <br />

    <table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.Plot_Size)</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
            <tr>                         
                <td>@Html.DisplayFor(modelItem => item.Plot_Size)</td>
                <td>
                    <a asp-area="" asp-asp-controller="PlotSize" asp-action="Edit" asp-route-id="@item.PlotSizeId" class="btn btn-info">Edit</a>
                    <a href="#" onclick="ConfirmDelete(@item.PlotSizeId)" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

</div>

<script>
    function ConfirmDelete(plotsizeid){
        if(confirm('Are You sure to delete this Plot Size')){
            window.location.href = '/PlotSize/Delete/' + plotsizeid
        }
    }
</script>